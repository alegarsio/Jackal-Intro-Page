<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= data.name %> - <%= data.motto %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #f8f8f8; }
        ::-webkit-scrollbar-thumb { background: #ddd; }
        ::-webkit-scrollbar-thumb:hover { background: #bbb; }
        
        body {
            font-family: 'Space Grotesk', sans-serif;
        }
        
        .gradient-text {
            background: linear-gradient(to right, #000000, #333333);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }
    </style>
</head>
<body class="bg-white text-neutral-900 antialiased leading-relaxed">

    <header class="min-h-[90vh] flex flex-col justify-center px-6 md:px-24 lg:px-32">
        <div class="max-w-4xl">
            <p class="text-lg md:text-xl text-neutral-400 mb-8 tracking-wide uppercase">
                <%= data.motto %>
            </p>
            <h1 class="text-6xl md:text-8xl font-bold tracking-tight mb-8 gradient-text">
                <%= data.name %>.
            </h1>
            <div class="inline-block bg-black bg-opacity-5 backdrop-blur-sm px-4 py-2 text-sm mb-8 tracking-wide">
                Latest: v<%= data.releases[0].version %>
            </div>
            <p class="text-xl md:text-2xl text-neutral-700 max-w-2xl leading-relaxed mb-12">
                <%= data.description %>
            </p>
            <div class="flex gap-6">
                <a href="#quick-start" class="bg-black text-white px-8 py-3 hover:bg-neutral-800 transition-all duration-300 tracking-wide">
                    Get Started
                </a>
                <a href="<%= data.repoLink %>" target="_blank" class="border border-black px-8 py-3 hover:bg-black hover:text-white transition-all duration-300 tracking-wide">
                    GitHub Repository
                </a>
            </div>
        </div>
    </header>

    <section id="vision" class="py-32 px-6 md:px-24 lg:px-32 bg-gradient-to-b from-white to-neutral-50">
        <div class="max-w-3xl">
            <h2 class="text-sm uppercase tracking-widest text-neutral-400 mb-6">Project Vision</h2>
            <p class="text-3xl md:text-4xl leading-tight font-light">
                <%= data.vision %>
            </p>
        </div>
    </section>

    <section id="features" class="py-32 px-6 md:px-24 lg:px-32">
        <div class="mb-20">
             <h2 class="text-4xl tracking-tight gradient-text">Core Features<span class="text-neutral-300">.</span></h2>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-16 gap-y-20">
            <% data.features.forEach(feature => { %>
                <div class="group flex flex-col pl-6 relative before:absolute before:left-0 before:top-0 before:w-px before:h-full before:bg-neutral-200 hover:before:bg-black before:transition-colors before:duration-300">
                    <h3 class="text-xl mb-4 tracking-tight"><%= feature.title %></h3>
                    <p class="text-neutral-500 leading-relaxed text-sm"><%= feature.desc %></p>
                </div>
            <% }) %>
        </div>
    </section>

    <section id="quick-start" class="flex flex-col lg:flex-row">
        <div class="lg:w-1/3 bg-black text-neutral-200 p-12 lg:p-24 flex flex-col justify-center">
            <h2 class="text-3xl text-white mb-16 tracking-tight">Quick Start<span class="text-neutral-700">.</span></h2>
            <div class="space-y-12">
                <div>
                    <p class="text-xs text-neutral-500 mb-3 uppercase tracking-wider">1. Clone & Build</p>
                    <div class="bg-neutral-900 p-6 text-sm overflow-x-auto rounded-sm">
                        <p class="text-emerald-400">$ git clone <%= data.repoLink %></p>
                        <p class="opacity-80">$ cd jackal</p>
                        <p class="opacity-80">$ make jackal</p>
                    </div>
                </div>
                <div>
                    <p class="text-xs text-neutral-500 mb-3 uppercase tracking-wider">2. Run a Program</p>
                    <div class="bg-neutral-900 p-6 text-sm overflow-x-auto rounded-sm">
                        <p class="opacity-80">$ ./jackal your_script.jackal</p>
                    </div>
                </div>
            </div>
        </div>
        <div class="lg:w-2/3 bg-neutral-50 p-12 lg:p-24">
            <h2 class="text-3xl mb-16 tracking-tight gradient-text">Syntax Preview<span class="text-neutral-300">.</span></h2>
            <div class="space-y-16 max-w-3xl">
                <div>
                    <h3 class="text-base tracking-tight mb-4 text-neutral-400">Object-Oriented</h3>
                    <pre class="bg-white p-8 font-mono text-sm overflow-x-auto shadow-sm border border-neutral-100"><span class="text-blue-600 font-bold">class</span> Jack {
    <span class="text-purple-600 font-bold">function</span> <span class="text-yellow-600">init</span>(name){
        <span class="text-red-500">this</span>.name = name;
    }

    <span class="text-purple-600 font-bold">function</span> <span class="text-blue-600">greet</span>(){
        <span class="text-purple-600 font-bold">print</span> <span class="text-green-600">"Hello "</span> + <span class="text-red-500">this</span>.name;
    }
}

<span class="text-blue-600 font-bold">let</span> jack = Jack(<span class="text-green-600">"Jack"</span>);
jack.greet();</pre>
                </div>
                <div>
                    <h3 class="text-base tracking-tight mb-4 text-neutral-400">Pattern Matching</h3>
                    <pre class="bg-white p-8 font-mono text-sm overflow-x-auto shadow-sm border border-neutral-100"><span class="text-blue-600 font-bold">let</span> score = 90;
<span class="text-purple-600 font-bold">match</span> (score) {
    90 => <span class="text-purple-600 font-bold">print</span> <span class="text-green-600">"Passed"</span>;
    75 => <span class="text-purple-600 font-bold">print</span> <span class="text-green-600">"Ok"</span>;
    <span class="text-blue-600 font-bold">default</span> => <span class="text-purple-600 font-bold">print</span> <span class="text-green-600">"Not in the list"</span>;
}</pre>
                </div>
            </div>
        </div>
    </section>

    <section id="releases" class="py-32 px-6 md:px-24 lg:px-32 bg-gradient-to-b from-white to-neutral-50">
        <div class="max-w-4xl mx-auto">
            <h2 class="text-4xl tracking-tight mb-20 text-center gradient-text">Release Notes<span class="text-neutral-300">.</span></h2>

            <div class="space-y-16 relative before:absolute before:inset-0 before:ml-5 before:-translate-x-px md:before:mx-auto md:before:translate-x-0 before:h-full before:w-px before:bg-gradient-to-b before:from-transparent before:via-neutral-200 before:to-transparent">
                <% data.releases.forEach(release => { %>
                    <div class="relative flex items-center justify-between md:justify-normal md:odd:flex-row-reverse group is-active">
                        <div class="flex items-center justify-center w-3 h-3 rounded-full bg-neutral-200 group-hover:bg-black transition-colors duration-300 shrink-0 md:order-1 md:group-odd:-translate-x-1/2 md:group-even:translate-x-1/2">
                        </div>
                        
                        <div class="w-[calc(100%-4rem)] md:w-[calc(50%-2.5rem)] bg-white p-8 shadow-sm hover:shadow-md transition-shadow duration-300">
                            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-6">
                                <h3 class="text-xl tracking-tight">v<%= release.version %></h3>
                                <time class="text-xs text-neutral-400 uppercase tracking-wider"><%= release.date %></time>
                            </div>
                            
                            <div class="mb-6">
                                <span class="inline-block bg-black bg-opacity-5 text-xs px-3 py-1.5 uppercase tracking-wider"><%= release.label %></span>
                            </div>
                            
                            <p class="text-neutral-600 mb-8 text-sm leading-relaxed">
                                <%= release.highlights %>
                            </p>

                            <ul class="space-y-3">
                                <% release.changes.forEach(change => { %>
                                    <li class="text-sm text-neutral-500 flex items-start group">
                                        <span class="text-[10px] mr-3 px-2 py-1 bg-neutral-50 text-neutral-400 uppercase tracking-wider min-w-[50px] text-center shrink-0 group-hover:bg-black group-hover:text-white transition-colors duration-300">
                                            <%= change.type %>
                                        </span>
                                        <span class="pt-0.5"><%= change.text %></span>
                                    </li>
                                <% }) %>
                            </ul>
                        </div>
                    </div>
                <% }) %>
            </div>

        </div>
    </section>

    <footer class="py-16 px-6 text-center">
        <p class="text-neutral-400 text-xs tracking-wider">
            Jackal Language Project &copy; <%= new Date().getFullYear() %>. Open Source for Open Scientists.
        </p>
    </footer>

    <script>
        // Memastikan halaman selalu mulai dari paling atas saat di-refresh atau dibuka pertama kali
        if (history.scrollRestoration) {
            history.scrollRestoration = 'manual';
        } else {
            window.onbeforeunload = function () {
                window.scrollTo(0, 0);
            }
        }
        
        // Scroll ke atas saat load
        window.onload = function() {
             window.scrollTo(0, 0);
        };
    </script>

</body>
</html>